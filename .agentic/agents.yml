# Baseline agent pool. Secrets are referenced by NAME (never a value).
agents:
  default:
    runtime: local
    model: { id: composer-2.5 }
    mcpServers:
      gh:
        use: github
        secrets:
          token: "GITHUB-ASTA-STARTER-KIT"
